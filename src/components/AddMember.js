import React from 'react';

const AddMember = (props) => (
    <form>
        <input type='text' />
        <input type='text' />
        <button>Add Member</button>
    </form>
);

export default AddMember;